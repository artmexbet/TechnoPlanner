// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: telegram_users.sql

package queries

import (
	"context"
)

const GetTelegramUserByID = `-- name: GetTelegramUserByID :one
SELECT id, telegram_id, username, first_name, last_name, created_at, updated_at
FROM telegram_users
WHERE telegram_id = $1
`

func (q *Queries) GetTelegramUserByID(ctx context.Context, telegramID int64) (TelegramUser, error) {
	row := q.db.QueryRow(ctx, GetTelegramUserByID, telegramID)
	var i TelegramUser
	err := row.Scan(
		&i.ID,
		&i.TelegramID,
		&i.Username,
		&i.FirstName,
		&i.LastName,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const SaveTelegramUser = `-- name: SaveTelegramUser :one
INSERT INTO telegram_users (telegram_id, username, first_name, last_name)
VALUES ($1, $2, $3, $4)
ON CONFLICT (telegram_id) DO UPDATE
SET username = EXCLUDED.username,
    first_name = EXCLUDED.first_name,
    last_name = EXCLUDED.last_name,
    updated_at = NOW()
RETURNING id, telegram_id, username, first_name, last_name, created_at, updated_at
`

type SaveTelegramUserParams struct {
	TelegramID int64
	Username   *string
	FirstName  *string
	LastName   *string
}

func (q *Queries) SaveTelegramUser(ctx context.Context, arg SaveTelegramUserParams) (TelegramUser, error) {
	row := q.db.QueryRow(ctx, SaveTelegramUser,
		arg.TelegramID,
		arg.Username,
		arg.FirstName,
		arg.LastName,
	)
	var i TelegramUser
	err := row.Scan(
		&i.ID,
		&i.TelegramID,
		&i.Username,
		&i.FirstName,
		&i.LastName,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
