router:
  address: 0.0.0.0
  port: 8080
broker:
  url: ""
  streams:
    something:
      name: SMTHING
      subjects:
        - "smthing.created"
      max_age: 1d
postgres:
  host: localhost
  port: 5433
  user: postgres
  password: postgres
  db: gateway
  timeout: 5s
telemetry:
  service_name: "gateway-service"
  jaeger_endpoint: "http://localhost:14268/api/traces"
  enabled: true