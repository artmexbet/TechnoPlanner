openapi: 3.1.0
info:
  title: Gateway Service API
  version: 1.0.0
  description: REST API exposed by the technoBro gateway service.
servers:
  - url: https://gateway.local
    description: Default environment
  - url: http://localhost:8080
    description: Local development
security:
  - bearerAuth: []
tags:
  - name: health
    description: Health and readiness probes
  - name: auth
    description: Authentication and token lifecycle
  - name: users
    description: User management
  - name: porters
    description: Porter management (stored in Requests service)
  - name: equipment
    description: Equipment catalog management
  - name: categories
    description: Equipment categories
  - name: requests
    description: Equipment request workflows
  - name: raw-requests
    description: Raw (bot) requests management
paths:
  /healthz:
    get:
      tags: [health]
      summary: Health probe
      security: []
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
  /ping:
    get:
      tags: [health]
      summary: Liveness probe
      security: []
      responses:
        '200':
          description: Service is alive
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /api/v1/users/register:
    post:
      tags: [auth, users]
      summary: Register a new user
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '500': { $ref: '#/components/responses/InternalError' }
  /api/v1/users/login:
    post:
      tags: [auth, users]
      summary: Login with credentials
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Token pair issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenPair'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /api/v1/users/refresh:
    post:
      tags: [auth, users]
      summary: Refresh token pair
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenPair'
      responses:
        '200':
          description: New token pair issued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenPair'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /api/v1/users/me:
    get:
      tags: [auth, users]
      summary: Get current user info
      description: Returns information about the currently authenticated user.
      responses:
        '200':
          description: Current user info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
  /api/v1/users/logout:
    post:
      tags: [auth, users]
      summary: Logout current session
      responses:
        '200':
          description: Logged out successfully
        '401': { $ref: '#/components/responses/Unauthorized' }
        '500': { $ref: '#/components/responses/InternalError' }
  /api/v1/porters:
    get:
      tags: [porters]
      summary: List porters
      description: >
        Returns the list of all porters stored in the Requests service.
        Requires admin role.
      responses:
        '200':
          description: Porter list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PorterListResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
    post:
      tags: [porters]
      summary: Create a new porter
      description: >
        Registers a new porter via the Auth service. On success the Auth service
        publishes a `UserCreated` event (role_id=2) which the Requests service
        persists automatically. A random password is generated automatically.
        Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PorterCreateRequest'
      responses:
        '201':
          description: Porter created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                    description: UUID of the created porter
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '500': { $ref: '#/components/responses/InternalError' }
  /api/v1/porters/{porterId}:
    parameters:
      - $ref: '#/components/parameters/PorterId'
    get:
      tags: [porters]
      summary: Get porter by ID
      description: Retrieves porter info from the Requests service. Requires admin role.
      responses:
        '200':
          description: Porter found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PorterResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [porters]
      summary: Update porter by ID
      description: >
        Updates the username and email of an existing porter via the Auth service.
        Returns full user info including email and timestamps. Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PorterUpdateRequest'
      responses:
        '200':
          description: Porter updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PorterResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [porters]
      summary: Delete porter by ID
      description: >
        Removes the porter from the Requests service storage and deletes the
        user account from the Auth service. Requires admin role.
      responses:
        '204': { description: Deleted }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/equipment/categories:
    get:
      tags: [categories]
      summary: List equipment categories
      responses:
        '200':
          description: Categories list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentCategoryListResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [categories]
      summary: Create equipment category
      description: Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EquipmentCategoryRequest'
      responses:
        '201':
          description: Category created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentCategoryResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /api/v1/equipment/categories/{categoryId}:
    parameters:
      - $ref: '#/components/parameters/CategoryId'
    put:
      tags: [categories]
      summary: Update category
      description: Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EquipmentCategoryRequest'
      responses:
        '200':
          description: Category updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentCategoryResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [categories]
      summary: Delete category
      description: Requires admin role.
      responses:
        '204': { description: Deleted }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/equipment:
    get:
      tags: [equipment]
      summary: List equipment
      responses:
        '200':
          description: Equipment list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/EquipmentResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
    post:
      tags: [equipment]
      summary: Create equipment
      description: Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EquipmentCreateRequest'
      responses:
        '201':
          description: Equipment created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /api/v1/equipment/{equipmentId}:
    parameters:
      - $ref: '#/components/parameters/EquipmentId'
    get:
      tags: [equipment]
      summary: Get equipment item
      responses:
        '200':
          description: Equipment details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      tags: [equipment]
      summary: Update equipment item
      description: Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EquipmentUpdateRequest'
      responses:
        '200':
          description: Updated equipment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EquipmentResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    delete:
      tags: [equipment]
      summary: Delete equipment
      description: Requires admin role.
      responses:
        '204': { description: Deleted }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/requests:
    get:
      tags: [requests]
      summary: List requests
      description: >
        Returns a list of equipment requests.

        - **Admin**: omit `responsible_id` to get all requests; pass `responsible_id` to filter by assigned porter.

        - **Porter**: must pass their own UUID as `responsible_id`; omitting it returns 403.
      parameters:
        - in: query
          name: responsible_id
          schema:
            type: string
            format: uuid
          description: >
            Filter by assigned responsible (porter) UUID.
            Admin only: omit to get all requests.
      responses:
        '200':
          description: Requests list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RequestListResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /api/v1/requests/{requestId}:
    parameters:
      - $ref: '#/components/parameters/RequestId'
    get:
      tags: [requests]
      summary: Get request by ID
      description: >
        Admin can access any request.
        Porter can only access requests assigned to them.
      responses:
        '200':
          description: Request details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RequestResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    patch:
      tags: [requests]
      summary: Partially update a request
      description: >
        Updates one or more fields of a request (status, address, schedule time,
        request text, responsible person). Only provided fields are changed.
        Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestUpdateRequest'
      responses:
        '200':
          description: Updated request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RequestResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/requests/{requestId}/responsible:
    parameters:
      - $ref: '#/components/parameters/RequestId'
    post:
      tags: [requests]
      summary: Assign responsible (porter) to request
      description: Assigns a porter to an equipment request. Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignResponsibleRequest'
      responses:
        '200':
          description: Assignment updated, returns the updated request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RequestResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/requests/{requestId}/history:
    parameters:
      - $ref: '#/components/parameters/RequestId'
    get:
      tags: [requests]
      summary: List request status history
      responses:
        '200':
          description: History entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RequestStatusHistoryListResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      tags: [requests]
      summary: Append status history entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestStatusUpdateRequest'
      responses:
        '201':
          description: History entry added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RequestStatusHistoryResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/raw-requests:
    get:
      tags: [raw-requests]
      summary: List raw (bot) requests
      description: Requires admin role.
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [new, processed]
          description: Filter by processing status. Omit to get all.
      responses:
        '200':
          description: Raw requests list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RawRequestListResponse'
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /api/v1/raw-requests/{rawRequestId}:
    parameters:
      - $ref: '#/components/parameters/RawRequestId'
    get:
      tags: [raw-requests]
      summary: Get raw request by ID
      description: Requires admin role.
      responses:
        '200':
          description: Raw request details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RawRequestResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
  /api/v1/raw-requests/{rawRequestId}/process:
    parameters:
      - $ref: '#/components/parameters/RawRequestId'
    post:
      tags: [raw-requests]
      summary: Process raw request into a formal request
      description: >
        Converts a raw Telegram bot request into a formal equipment request.
        The raw request status changes to "processed" and a new Request is created.
        Requires admin role.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RawRequestProcessRequest'
      responses:
        '201':
          description: Request created, raw request updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProcessRawRequestResponse'
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '500': { $ref: '#/components/responses/InternalError' }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    RequestId:
      name: requestId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    EquipmentId:
      name: equipmentId
      in: path
      required: true
      schema:
        type: integer
        format: int32
    CategoryId:
      name: categoryId
      in: path
      required: true
      schema:
        type: integer
        format: int32
    PorterId:
      name: porterId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: UUID of the porter
    RawRequestId:
      name: rawRequestId
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: UUID of the raw request
  responses:
    BadRequest:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthorized:
      description: Authentication required or failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Access denied
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
        details:
          type: string
    RegisterRequest:
      type: object
      required: [username, password, email]
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 30
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 6
    RegisterResponse:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
    TokenPair:
      type: object
      required: [access_token, refresh_token]
      properties:
        access_token:
          type: string
        refresh_token:
          type: string
    MeResponse:
      type: object
      required: [id, username, email, role]
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
          format: email
        role:
          type: string
          enum: [admin, porter]
    PorterResponse:
      type: object
      description: >
        Porter info. `id` and `username` are always present.
        `email`, `created_at`, `updated_at` are populated when the response
        originates from the Auth service (e.g. after PUT).
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        email:
          type: string
          format: email
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    PorterListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PorterResponse'
    PorterCreateRequest:
      type: object
      required: [username]
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 30
          example: porter1
        email:
          type: string
          format: email
          nullable: true
          description: >
            Optional email. If omitted, a placeholder address is generated automatically
            in the format `<username>@porter.local`.
          example: porter1@example.com
    PorterUpdateRequest:
      type: object
      required: [username, email]
      properties:
        username:
          type: string
          minLength: 3
          maxLength: 30
          example: porter1_updated
        email:
          type: string
          format: email
          example: porter1_updated@example.com
    EquipmentCategoryResponse:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        created_by:
          type: string
          format: uuid
          nullable: true
        updated_by:
          type: string
          format: uuid
          nullable: true
        deleted_at:
          type: string
          format: date-time
          nullable: true
    EquipmentCategoryRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        description:
          type: string
          nullable: true
    EquipmentCategoryListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentCategoryResponse'
    EquipmentResponse:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        quantity:
          type: integer
          format: int32
          minimum: 0
        categories:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentCategoryResponse'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        created_by:
          type: string
          format: uuid
          nullable: true
        updated_by:
          type: string
          format: uuid
          nullable: true
        deleted_at:
          type: string
          format: date-time
          nullable: true
    EquipmentCreateRequest:
      type: object
      required: [name, quantity]
      properties:
        name:
          type: string
        description:
          type: string
          nullable: true
        quantity:
          type: integer
          format: int32
          minimum: 0
        category_ids:
          type: array
          items:
            type: integer
            minimum: 1
    EquipmentUpdateRequest:
      allOf:
        - $ref: '#/components/schemas/EquipmentCreateRequest'
    RequestEquipmentItem:
      type: object
      description: >
        Equipment attached to a request. `quantity` is the amount requested
        (from equipment_to_requests), not the total stock.
      properties:
        equipment_id:
          type: integer
        name:
          type: string
        description:
          type: string
          nullable: true
        quantity:
          type: integer
          format: int32
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    RequestStatus:
      type: string
      enum:
        - canceled
        - pending
        - assigned
        - in_progress
        - completed
        - rejected
    RequestResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        request_text:
          type: string
          nullable: true
        status:
          $ref: '#/components/schemas/RequestStatus'
        schedule_time:
          type: string
        end_time:
          type: string
          format: date-time
        address:
          type: string
        responsible_id:
          type: string
          format: uuid
          nullable: true
          description: UUID of the assigned porter (responsible)
        equipment:
          type: array
          items:
            $ref: '#/components/schemas/RequestEquipmentItem'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        created_by:
          type: string
          format: uuid
          nullable: true
        updated_by:
          type: string
          format: uuid
          nullable: true
        deleted_at:
          type: string
          format: date-time
          nullable: true
    RequestListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RequestResponse'
    RequestUpdateRequest:
      type: object
      description: Partial update of a request. All fields are optional.
      properties:
        request_text:
          type: string
          nullable: true
        status:
          $ref: '#/components/schemas/RequestStatus'
        schedule_time:
          type: string
          nullable: true
        address:
          type: string
          nullable: true
        responsible_id:
          type: string
          format: uuid
          nullable: true
          description: UUID of the porter to assign (null to unassign)
    AssignResponsibleRequest:
      type: object
      required: [responsible_id]
      properties:
        responsible_id:
          type: string
          format: uuid
          description: UUID of the porter to assign
    RequestStatusUpdateRequest:
      type: object
      required: [status]
      properties:
        status:
          $ref: '#/components/schemas/RequestStatus'
        comment:
          type: string
          nullable: true
    RequestStatusHistoryResponse:
      type: object
      properties:
        id:
          type: integer
          format: int32
        request_id:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/RequestStatus'
        comment:
          type: string
          nullable: true
        changed_by:
          type: string
          format: uuid
          nullable: true
        changed_at:
          type: string
          format: date-time
    RequestStatusHistoryListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RequestStatusHistoryResponse'
    RawRequestResponse:
      type: object
      description: Raw request coming from the Telegram bot
      properties:
        id:
          type: string
          format: uuid
        telegram_id:
          type: integer
          format: int64
          description: Telegram user ID
        username:
          type: string
          description: Telegram username
        first_name:
          type: string
        last_name:
          type: string
          nullable: true
        raw_text:
          type: string
          description: Raw message text from the bot
        status:
          type: string
          enum: [new, processed]
          description: Processing status of the raw request
        processed_request_id:
          type: string
          format: uuid
          nullable: true
          description: ID of the formal Request created from this raw request
        created_at:
          type: string
          format: date-time
    RawRequestListResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RawRequestResponse'
    EquipmentInReq:
      type: object
      required: [id, quantity]
      properties:
        id:
          type: integer
          description: Equipment ID
        quantity:
          type: integer
          minimum: 1
          description: Quantity requested
    RawRequestProcessRequest:
      type: object
      required: [schedule_time, address]
      description: Payload to convert a raw bot request into a formal equipment request
      properties:
        request_text:
          type: string
          nullable: true
          description: Optional override for the request description
        schedule_time:
          type: string
          format: date-time
          description: Scheduled date/time for the request
        address:
          type: string
          description: Delivery/work address
        equipment_string:
          type: string
          nullable: true
          description: Free-form equipment list string (alternative to structured equipments)
        equipments:
          type: array
          items:
            $ref: '#/components/schemas/EquipmentInReq'
          description: Structured list of equipment with quantities
    ProcessRawRequestResponse:
      type: object
      properties:
        request:
          $ref: '#/components/schemas/RequestResponse'
        raw_request:
          $ref: '#/components/schemas/RawRequestResponse'
