version: "2"
sql:
  - engine: "postgresql"
    schema: "../../../migrations/"
    queries:
      - "./"
    gen:
      go:
        package: "queries"
        out: "."
        sql_package: "pgx/v5"
        emit_exported_queries: true